---
repos_dnf_google_chrome_repo: "https://dl.google.com/linux/chrome/rpm/stable/x86_64"

# Internal Red Hat / work-profile repo URLs — set in config.yml (gitignored).
# When defined, these are used by the acli and redhat-internal repo tasks.
# repos_dnf_acli_baseurl: ""
# repos_dnf_redhat_baseurl: ""
# repos_dnf_redhat_gpgkey: ""
