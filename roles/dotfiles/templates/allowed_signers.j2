# {{ ansible_managed }}
# Git commit signing allowed signers
# See: https://git-scm.com/docs/gitformat-signature
{% if ssh_git_signing_pubkey | trim | length > 0 %}
{{ dotfiles_user_email_work }},{{ dotfiles_user_email_personal }} namespaces="git" {{ ssh_git_signing_pubkey }}
{% endif %}
