# {{ ansible_managed }}
[Unit]
Description=Daily AIDE file integrity check

[Timer]
OnCalendar={{ system_aide_check_calendar }}
Persistent=true  # run missed check on next boot if system was off at scheduled time
RandomizedDelaySec={{ system_aide_check_random_delay }}

[Install]
WantedBy=timers.target
