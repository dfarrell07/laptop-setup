---
containers_auth_file: "{{ ansible_user_dir }}/.config/containers/auth.json"

containers_registries:
  - registry.redhat.io
  - quay.io

containers_config_dir: "{{ ansible_user_dir }}/.config/containers"
