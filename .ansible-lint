---
profile: production

exclude_paths:
  - collections/
  - .github/
  - molecule/shared/  # task-include files; not standalone playbooks — linted via scenario verify.yml inclusion

skip_list:
  - yaml[line-length]  # yamllint handles this with 140 char limit
