---
# molecule/shared/csb-report-post.yml
# Shared post-converge task: generate the CSB compatibility report.
# Included by fedora, rocky, and debian converge.yml post_tasks with tags: [always].
- name: Generate CSB compatibility report
  ansible.builtin.include_tasks: "{{ lookup('env', 'MOLECULE_PROJECT_DIRECTORY') }}/common/tasks/csb-report.yml"
